/**
 * 에셋 스튜디오 - re-exported from ./studio/, where the tab now lives as a
 * package (store · left lists · gen · editors · selector). This shell keeps
 * shell.ts's import path stable.
 */
export { renderStudioTab, noteStudioLeft } from './studio/index';
